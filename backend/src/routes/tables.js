import { Router } from 'express';import { prisma } from '../lib/prisma.js';import { asyncHandler,HttpError } from '../lib/http.js';import { auth,roles } from '../middleware/auth.js';
const router=Router();router.use(auth);const include={tabs:{where:{status:'OPEN'},include:{customer:true,waiter:{select:{id:true,name:true}},orders:{where:{status:{not:'CANCELLED'}},include:{items:true}}}}};
router.get('/',asyncHandler(async(req,res)=>{const tables=await prisma.restaurantTable.findMany({where:{restaurantId:req.user.restaurantId},include,orderBy:{number:'asc'}});res.json(tables)}));
router.post('/',roles('ADMIN','MANAGER'),asyncHandler(async(req,res)=>{const table=await prisma.restaurantTable.create({data:{restaurantId:req.user.restaurantId,number:String(req.body.number),seats:Number(req.body.seats||4),status:req.body.status||'AVAILABLE',note:req.body.note||null}});req.app.locals.io.to(req.user.restaurantId).emit('table:changed',table);res.status(201).json(table)}));
router.put('/:id',roles('ADMIN','MANAGER'),asyncHandler(async(req,res)=>{const current=await prisma.restaurantTable.findFirst({where:{id:req.params.id,restaurantId:req.user.restaurantId}});if(!current)throw new HttpError(404,'Mesa não encontrada');const table=await prisma.restaurantTable.update({where:{id:current.id},data:{number:req.body.number,seats:req.body.seats?Number(req.body.seats):undefined,status:req.body.status,note:req.body.note}});req.app.locals.io.to(req.user.restaurantId).emit('table:changed',table);res.json(table)}));
router.delete('/:id',roles('ADMIN','MANAGER'),asyncHandler(async(req,res)=>{if(await prisma.tab.count({where:{tableId:req.params.id,status:'OPEN'}}))throw new HttpError(409,'Feche a comanda antes de excluir a mesa');await prisma.restaurantTable.deleteMany({where:{id:req.params.id,restaurantId:req.user.restaurantId}});req.app.locals.io.to(req.user.restaurantId).emit('table:deleted',{id:req.params.id});res.status(204).end()}));
router.post('/:id/open',asyncHandler(async(req,res)=>{const table=await prisma.restaurantTable.findFirst({where:{id:req.params.id,restaurantId:req.user.restaurantId}});if(!table)throw new HttpError(404,'Mesa não encontrada');const existing=await prisma.tab.findFirst({where:{tableId:table.id,status:'OPEN'},include:{orders:{include:{items:true}}}});if(existing)return res.json(existing);const tab=await prisma.$transaction(async tx=>{await tx.restaurantTable.update({where:{id:table.id},data:{status:'OCCUPIED'}});return tx.tab.create({data:{restaurantId:req.user.restaurantId,tableId:table.id,customerId:req.body.customerId||null,waiterId:req.user.id},include:{table:true,customer:true,orders:true}})});req.app.locals.io.to(req.user.restaurantId).emit('tab:opened',tab);res.status(201).json(tab)}));
router.post('/:id/close',asyncHandler(async(req,res)=>{const tab=await prisma.tab.findFirst({where:{tableId:req.params.id,status:'OPEN'},include:{orders:{include:{items:true}}}});if(!tab)throw new HttpError(404,'Comanda aberta não encontrada');const subtotal=tab.orders.filter(o=>o.status!=='CANCELLED').flatMap(o=>o.items).reduce((s,i)=>s+Number(i.unitPrice)*i.quantity,0);const fee=subtotal*Number(req.body.serviceFeePercent??10)/100;const closed=await prisma.$transaction(async tx=>{const result=await tx.tab.update({where:{id:tab.id},data:{status:'CLOSED',subtotal,serviceFee:fee,total:subtotal+fee,closedAt:new Date()}});await tx.restaurantTable.update({where:{id:req.params.id},data:{status:'AVAILABLE'}});return result});req.app.locals.io.to(req.user.restaurantId).emit('tab:closed',closed);res.json(closed)}));export default router;
