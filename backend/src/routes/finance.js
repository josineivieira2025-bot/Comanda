import { Router } from 'express';import { prisma } from '../lib/prisma.js';import { asyncHandler,HttpError } from '../lib/http.js';import { auth,roles } from '../middleware/auth.js';
const router=Router();router.use(auth);router.get('/summary',asyncHandler(async(req,res)=>{const start=new Date();start.setHours(0,0,0,0);const [session,payments]=await Promise.all([prisma.cashSession.findFirst({where:{restaurantId:req.user.restaurantId,status:'OPEN'}}),prisma.payment.findMany({where:{restaurantId:req.user.restaurantId,createdAt:{gte:start}},orderBy:{createdAt:'desc'}})]);const total=payments.reduce((s,p)=>s+(p.type==='WITHDRAWAL'?-1:1)*Number(p.amount),0);res.json({session,payments,total,byMethod:Object.fromEntries(['CASH','PIX','CARD','VOUCHER'].map(m=>[m,payments.filter(p=>p.method===m&&p.type==='SALE').reduce((s,p)=>s+Number(p.amount),0)]))})}));
router.post('/cash/open',roles('ADMIN','MANAGER','CASHIER'),asyncHandler(async(req,res)=>{if(await prisma.cashSession.findFirst({where:{restaurantId:req.user.restaurantId,status:'OPEN'}}))throw new HttpError(409,'Já existe um caixa aberto');res.status(201).json(await prisma.cashSession.create({data:{restaurantId:req.user.restaurantId,openedById:req.user.id,openingAmount:Number(req.body.openingAmount||0)}}))}));
router.post('/cash/close',roles('ADMIN','MANAGER','CASHIER'),asyncHandler(async(req,res)=>{const session=await prisma.cashSession.findFirst({where:{restaurantId:req.user.restaurantId,status:'OPEN'}});if(!session)throw new HttpError(404,'Caixa aberto não encontrado');res.json(await prisma.cashSession.update({where:{id:session.id},data:{status:'CLOSED',closingAmount:Number(req.body.closingAmount),closedById:req.user.id,closedAt:new Date()}}))}));
router.post('/payments',roles('ADMIN','MANAGER','CASHIER'),asyncHandler(async(req,res)=>{const session=await prisma.cashSession.findFirst({where:{restaurantId:req.user.restaurantId,status:'OPEN'}});const payment=await prisma.payment.create({data:{restaurantId:req.user.restaurantId,userId:req.user.id,cashSessionId:session?.id,tabId:req.body.tabId||null,type:req.body.type,method:req.body.method,amount:Number(req.body.amount),description:req.body.description}});res.status(201).json(payment)}));export default router;
