import { Router } from 'express';import bcrypt from 'bcryptjs';import { prisma } from '../lib/prisma.js';import { asyncHandler,HttpError,publicUser } from '../lib/http.js';import { auth,roles } from '../middleware/auth.js';
const router=Router();router.use(auth,roles('ADMIN','MANAGER'));router.get('/',asyncHandler(async(req,res)=>res.json((await prisma.user.findMany({where:{restaurantId:req.user.restaurantId},orderBy:{name:'asc'}})).map(publicUser))));router.post('/',asyncHandler(async(req,res)=>{if(await prisma.user.findUnique({where:{email:req.body.email}}))throw new HttpError(409,'E-mail já cadastrado');const user=await prisma.user.create({data:{restaurantId:req.user.restaurantId,name:req.body.name,email:req.body.email,passwordHash:await bcrypt.hash(req.body.password,12),role:req.body.role}});res.status(201).json(publicUser(user))}));router.patch('/:id',asyncHandler(async(req,res)=>{const user=await prisma.user.findFirst({where:{id:req.params.id,restaurantId:req.user.restaurantId}});if(!user)throw new HttpError(404,'Usuário não encontrado');res.json(publicUser(await prisma.user.update({where:{id:user.id},data:{name:req.body.name,role:req.body.role,active:req.body.active}})))}));export default router;
