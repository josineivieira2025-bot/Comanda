import { useEffect,useState } from 'react';import { Search,ShoppingBag,ArrowLeft } from 'lucide-react';import { Link } from 'react-router-dom';import { publicApi } from '../services/api';import { money } from '../services/AppContext';
export default function PublicMenu(){const [menu,setMenu]=useState(null);const [search,setSearch]=useState('');useEffect(()=>{publicApi(`/menu/${import.meta.env.VITE_RESTAURANT_SLUG||'seu-restaurante'}`).then(setMenu).catch(()=>setMenu({error:true,products:[]}))},[]);if(!menu)return <div className="app-loading">Carregando cardápio...</div>;const products=menu.products.filter(p=>p.name.toLowerCase().includes(search.toLowerCase()));return <div className="public-page"><div className="public-nav"><Link to="/"><ArrowLeft/> Gestão</Link><div className="public-brand">orbe</div><button><ShoppingBag/> Cardápio</button></div><section className="menu-hero"><small>{menu.name?.toUpperCase()}</small><h1>Comida memorável,<br/>feita para este momento.</h1><p>Cardápio atualizado diretamente pela operação.</p><div className="public-search"><Search/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Buscar no cardápio"/></div></section><main className="public-menu-grid">{products.map(p=><article key={p.id}><img src={p.imageUrl}/><div><span>{p.category.name}</span><h2>{p.name}</h2><p>{p.description||'Preparado com ingredientes selecionados pela nossa cozinha.'}</p><b>{money(p.price)}</b></div></article>)}</main></div>}
