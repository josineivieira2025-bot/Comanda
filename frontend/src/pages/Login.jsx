import { useState } from 'react';import { Navigate,useLocation,useNavigate } from 'react-router-dom';import { ArrowRight,LockKeyhole,Mail } from 'lucide-react';import { useApp } from '../services/AppContext';
export default function Login(){const {user,login,register}=useApp();const nav=useNavigate();const loc=useLocation();const [mode,setMode]=useState('login');const [form,setForm]=useState({name:'',restaurantName:'',email:'admin@orbe.com',password:'admin123'});const [error,setError]=useState('');const [loading,setLoading]=useState(false);if(user)return <Navigate to="/"/>;async function submit(e){e.preventDefault();setLoading(true);setError('');try{if(mode==='login')await login(form.email,form.password);else await register(form);nav(loc.state?.from||'/')}catch(err){setError(err.message)}finally{setLoading(false)}}return <div className="login-page"><section className="login-brand"><div className="brand"><div className="brand-mark"><span/><span/><span/></div><b>orbe</b></div><div><small>RESTAURANT OPERATING SYSTEM</small><h1>Operação sob controle.<br/>Decisões em tempo real.</h1><p>Mesas, cozinha, estoque e caixa conectados em uma plataforma.</p></div></section><section className="login-form-wrap"><form onSubmit={submit}><small>ACESSO SEGURO</small><h2>{mode==='login'?'Bem-vindo de volta':'Crie sua operação'}</h2><p>{mode==='login'?'Entre com suas credenciais para continuar.':'O primeiro usuário será administrador.'}</p>{mode==='register'&&<><label>Seu nome<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></label><label>Restaurante<input value={form.restaurantName} onChange={e=>setForm({...form,restaurantName:e.target.value})} required/></label></>}<label>E-mail<div className="input-icon"><Mail/><input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></div></label><label>Senha<div className="input-icon"><LockKeyhole/><input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required minLength="6"/></div></label>{error&&<div className="form-error">{error}</div>}<button className="primary login-submit" disabled={loading}>{loading?'Aguarde...':mode==='login'?'Entrar na operação':'Criar conta'}<ArrowRight/></button><button type="button" className="mode-link" onClick={()=>setMode(mode==='login'?'register':'login')}>{mode==='login'?'Ainda não possui conta? Cadastre-se':'Já possui conta? Entrar'}</button></form></section></div>}
